fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
pillow==11.1.0
numpy==2.2.1
opencv-python-headless==4.11.0.86
# On Windows and macOS, plain `pip install -r requirements.txt` already gives the CPU
# build of PyTorch, which is what this app needs — inference runs on CPU.
#
# On Linux, PyPI's default torch wheel bundles the CUDA runtime (~2.5 GB of GPU libraries
# we never use). The Dockerfile therefore installs the CPU-only wheels from
# download.pytorch.org BEFORE this file is processed; pip then sees torch 2.6.0+cpu as
# already satisfying the pin below and leaves it alone.
torch==2.6.0
torchvision==0.21.0
